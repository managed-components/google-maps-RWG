{
  "name": "Google Maps - Reserve with Google",
  "description": "Google Maps - Reserve with Google",
  "namespace": "google-maps-rwg",
  "icon": "assets/google-maps-logo.svg",
  "categories": ["Advertising", "Miscellaneous"],
  "provides": ["events"],
  "allowCustomFields": true,
  "permissions": {
    "execute_unsafe_scripts": {
      "description": "Sets Google Maps RWG cookie directly through client-side scripts.",
      "required": true
    }
  }
}
